discord.py>=2.3.2
aiosqlite>=0.19.0
PyYAML>=6.0
python-dotenv>=1.0.0

# Optional extras (uncomment to enable features):
# yt-dlp>=2024.4.9        # music playback
# PyNaCl>=1.5.0           # voice encryption (required for music)
# aiohttp>=3.9.0          # web integrations (already a discord.py dep)
# Pillow>=10.0.0          # rank card image generation
